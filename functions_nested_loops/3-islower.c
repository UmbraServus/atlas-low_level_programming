#include "main.h"

/**
 * _islower- checks for lowercase
 * @a: the char that is referenced for the function
 *
 * Return: 1 if true and 0 if false.
 *
 */
int _islower(int a)
{
	if (a >= 97 && a <= 122)

{
	return (1);
}

return (0);

	}
