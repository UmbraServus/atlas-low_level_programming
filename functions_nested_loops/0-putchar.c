#include <stdio.h>
/** main - entry point
 *
 * return: 0
 */

int main()
{
	int word;

	word = '_';
	putchar(word);
	word = 'p';
	putchar(word);
	word = 'u';
	putchar(word);
	word = 't';
	putchar(word);
	word = 'c';
	putchar(word);
	word = 'h';
	putchar(word);
	word = 'a';
	putchar(word);
	word = 'r';
	putchar(word);
	putchar('\n');
	return(0);
}

