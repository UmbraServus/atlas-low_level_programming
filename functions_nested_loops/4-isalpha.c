#include "main.h"
/**
 * _isalpha- checks if a char is a letter aA - zZ
 * @a:
 *
 * return: 1 if true and 0 if false
 */

int _isalpha(int a)
{
	if (a <= 97 && a >= 122) || (a <= 65 && a >= 90);

	{
		return (1);

	}

	return(0);

}
